[package]
name = "easy-minecraft-server-installer"
version = "0.1.0"
edition = "2024"

[dependencies]
inquire = "0.9.4"
reqwest = { version = "0.13.2", features = ["blocking", "json"] }
serde = { version = "1.0.228",  features = ["derive"] }
